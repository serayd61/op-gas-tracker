# RPC URL for Optimism (optional, defaults to public endpoint)
RPC_URL=https://mainnet.optimism.io

# Server port (optional, defaults to 3000)
PORT=3000

# Gas price update interval in milliseconds (optional, defaults to 12000 - 1 block)
UPDATE_INTERVAL=12000

# Enable debug logging (optional)
DEBUG=false